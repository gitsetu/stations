{{> menu active="dashboard"}}

<section class="section">
  <h2 class="title {{> class-brand}}">{{title}}</h2>
  <h4 class="subtitle has-text-weight-light is-6">{{numberOfStations}} stations, {{numberOfReports}} reports</h4>
  <div class="field">
    <br>
  </div>
  {{> map}}
  {{> add-station}}
  {{> list-stations}}

</section>