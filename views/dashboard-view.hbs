{{> menu active="dashboard"}}

<section class="section">
  <h1 class="title is-capitalized has-text-weight-light">{{title}}</h1>
  <h2 class="subtitle">Total: {{numberOfStations}} stations, {{numberOfReports}} reports</h2>
  <div class="field">
  </div>

  {{> list-stations}}
  {{> add-station}}
</section>